version: '3.8'

services:
  orthus-relay:
    privileged: true
    network_mode: "host"
    image: orthus-relay:latest
    ports:
      - "5454:5454"
    environment:
      - NODE_ENV=prod

  orthus-app:
    privileged: true
    network_mode: "host"
    volumes:
      - /dev/:/dev/
      - /data/:/tmp/
    image: orthus-app:latest
    ports:
      - "8000:8000"
    environment:
      - STREAM_NAME=webcam
      - STREAM_HOST=localhost
      - STREAM_PORT=3131
      - INFPS=30
      - INRES=640x480
      - OUTRES=640x480
      - OUTFPS=30
      - BITRATE=1024k
      - THREADS=0
      - VSYNC=2
      - SEGMENT_TIME=10
      - VIDEO_INDEX=0
      - ARCHIVE=1
      - GRAYSCALE=0
      - RECORD_DIRECTORY=/data
      - DEBUG=true